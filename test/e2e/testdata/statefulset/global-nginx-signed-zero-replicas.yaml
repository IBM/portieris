apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: global-nginx-signed-zero
  labels:
    app: global-nginx-signed-zero
    test: "true"
spec:
  replicas: 0
  serviceName: nginx
  selector:
    matchLabels:
      app: global-nginx-signed-zero
  template:
    metadata:
      labels:
        app: global-nginx-signed-zero
    spec:
      containers:
      - name: nginx
        image: icr.io/portieris-authn/nginx:signed
        ports:
        - containerPort: 80

# Made with Bob
