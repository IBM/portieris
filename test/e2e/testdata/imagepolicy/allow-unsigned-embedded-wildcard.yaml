apiVersion: portieris.cloud.ibm.com/v1
kind: ImagePolicy
metadata:
  name: allow-unsigned-embedded-wildcard
spec:
   repositories:
    - name: "de.icr.io/*/nginx"
      policy:
        trust:
          enabled: false
